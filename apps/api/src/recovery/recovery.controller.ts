import { Controller, Get, Param, Query } from '@nestjs/common'; import { RecoveryService } from './recovery.service'; import { ListRecoveryCasesDto } from './dto/list-recovery-cases.dto';
@Controller('merchants/:merchantId/recovery-cases') export class RecoveryController { constructor(private readonly service:RecoveryService){} @Get() all(@Param('merchantId') merchantId:string,@Query() query:ListRecoveryCasesDto){return this.service.findAll(merchantId,query);} @Get(':id') one(@Param('merchantId') merchantId:string,@Param('id') id:string){return this.service.findOne(merchantId,id);} }
